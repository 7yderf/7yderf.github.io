# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist
*.zip

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
.claude
.codex

# Local env files
.env
.env.*
!.env.example

# Raw design assets (Drive export) — no confundir con src/assets (codigo)
/assets

# Respaldos que Eureka escribe junto a project-memory. Son dos formas: el .bak
# que reescribe en cada mutacion, y el .finalize-<hash>.bak que deja al cerrar
# una tarea. El comodin cubre ambas y las que agregue despues.
project-memory-v1.json.*.bak
project-memory-v1.json.bak


